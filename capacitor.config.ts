import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.umamusical',
  appName: 'uma-musical-app',
  webDir: 'build'
};

export default config;
